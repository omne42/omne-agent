{
  "name": "@omne/omne-client",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Minimal Node.js client for omne-app-server (JSON-RPC over stdio).",
  "exports": "./src/index.js",
  "scripts": {
    "test": "node --test ./tests/*.test.mjs"
  },
  "engines": {
    "node": ">=18"
  }
}
