# @codepm/pm-client

Minimal Node.js client for `pm-app-server` (JSON-RPC over stdio).

Local smoke (requires `pm-app-server` on PATH, or env override):

- `CODE_PM_APP_SERVER_BIN=target/debug/pm-app-server node ./packages/pm-client/examples/basic.mjs`

